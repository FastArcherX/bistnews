# BIST News environment example
# Copy this file to .env and fill the values

# Google OAuth client ID (public identifier)
# Example format: 1234567890-abcdefghijklmnopqrstuvwxyz.apps.googleusercontent.com
# IMPORTANT: For Google Sign-In in production you need a proper domain with HTTPS
# (IP origins are not accepted by Google). For local dev, http://localhost:8080 is fine.
# Do NOT put client_secret in this file or any frontend code.
GOOGLE_CLIENT_ID=427696701817-g0ucnndo8htf3dhg5m8sd99ud4i9vfvd.apps.googleusercontent.com

# Backend port (default 3001)
PORT=3001

# CORS origins (comma separated)
# Include your dev origins and, if using a Raspberry Pi or production domain, add them here.
# Examples:
#  - Local dev frontend: http://localhost:8080 (recommended)
#  - Optional alt dev: http://127.0.0.1:8080
#  - Backend direct (if you open the app from the backend port): http://localhost:3001
#  - Root origins (when serving frontend on port 80 via Nginx): http://localhost,http://127.0.0.1
#  - Raspberry Pi LAN IP (replace with your Pi IP): http://192.168.1.23
#  - Production (HTTPS strongly recommended): https://your-domain.example
CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080,http://localhost:3001,http://127.0.0.1:3001,http://localhost,http://127.0.0.1

# Allowed email domain for Google sign-in (lowercase)
# Only users with emails ending in this domain can authenticate.
ALLOWED_DOMAIN=britishschool-timisoara.ro

# This app reads settings from a .env file at startup (via dotenv).
# Copy this file to .env and adjust values for your environment.
